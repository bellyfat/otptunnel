.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install otptunnel

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv otptunnel
    $ pip install otptunnel
